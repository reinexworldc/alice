[
  {
    "type": "function",
    "function": {
      "name": "get_directory",
      "description": "List files and directories at a given path",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to a directory (relative or absolute)"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_lines",
      "description": "Get the number of lines in a file",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to a file (relative or absolute)"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "review_code",
      "description": "Get specific lines from a file by line number range",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to a file (relative or absolute)"
          },
          "start": {
            "type": "integer",
            "description": "Starting line number (inclusive, 1-indexed)"
          },
          "end": {
            "type": "integer",
            "description": "Ending line number (inclusive, 1-indexed)"
          }
        },
        "required": ["path", "start", "end"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "apply_patch",
      "description": "Replace, insert, or delete lines in a file. All line numbers refer to the original file, before any edits are applied.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to a file (relative or absolute)"
          },
          "new_lines": {
            "type": "array",
            "description": "List of line edits. Operation defaults to replace for backward compatibility. Include a trailing \\n in content when the inserted or replacement text should end with a line break.",
            "items": {
              "type": "object",
              "properties": {
                "line": {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Target line in the original file (1-indexed). insert_before also accepts one line past the end to append."
                },
                "operation": {
                  "type": "string",
                  "enum": ["replace", "insert_before", "insert_after", "delete"],
                  "default": "replace",
                  "description": "Edit operation. content is ignored for delete."
                },
                "content": {
                  "type": "string",
                  "description": "Text used by replace or insert operations. It may contain multiple lines."
                }
              },
              "required": ["line"]
            }
          }
        },
        "required": ["path", "new_lines"]
      }
    }
  }
]
